# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/

# Data: raw pulls and the merged panel ARE committed, so a reviewer can rerun
# analysis/lead_lag.py and analysis/backtest.py end to end without network
# access (several fetchers need a GITHUB_TOKEN or a non-rate-limited IP).

# Everything else (raw data, the merged panel, generated figures, the PDF
# report) is committed on purpose so the submission is self-contained.

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
